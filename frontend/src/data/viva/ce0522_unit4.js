// frontend/src/data/viva/ce0522_unit4.js
/**
 * CE0522: Web Technology — Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting
 * Comprehensive Viva Questions & Solutions
 */

export const CE0522_UNIT4_QUESTIONS = [
  {
    id: 'ce0522-u4-q1',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'basic',
    questionNumber: 'Q.1',
    question: 'What is PHP? Explain its server-side execution model and compare echo vs print.',
    shortAnswer: 'PHP (Hypertext Preprocessor) is an open-source, interpreted, server-side scripting language designed for web development. PHP code is executed on the web server (Apache/Nginx); the server processes the script, queries databases if needed, and returns plain rendered HTML/JSON to the client browser. `echo` returns no value (void) and can output multiple comma-separated arguments; `print` returns 1 and accepts only a single argument.',
    detailedAnswer: '1. PHP Execution Lifecycle:\n   - Step 1: Client sends an HTTP request for a `.php` file (e.g., `index.php`).\n   - Step 2: The Web Server (Apache via `mod_php` or Nginx via PHP-FPM) intercepts the request and invokes the PHP Zend Engine.\n   - Step 3: PHP parses and executes the script, running database queries or manipulating sessions.\n   - Step 4: Output generated by `echo` or HTML markup is assembled into an HTTP response.\n   - Step 5: Web server transmits plain HTML/JSON back to the client. The browser never sees the raw PHP source code.\n\n2. `echo` vs `print` Differences:\n   - Return Value: `echo` has no return value (void). `print` always returns integer `1`, allowing it to be used inside expressions (e.g., `$x = print("Hi");`).\n   - Arguments: `echo` can output multiple comma-separated strings (`echo "A", "B", "C";`). `print` takes only one argument.\n   - Performance: `echo` is marginally faster than `print` because it does not return a value.',
    keyPoints: [
      'Server-side scripting: Client only receives rendered HTML/CSS/JS, never PHP source code.',
      'Enclosed in `<?php ... ?>` tags.',
      '`echo` returns void and accepts multiple arguments (`echo $a, $b;`).',
      '`print` accepts one argument and returns 1.',
      '`echo` is faster than `print`.'
    ],
    example: `<?php\n  $course = "Web Technology";\n  $sem = 5;\n  \n  // Using echo\n  echo "Welcome to ", $course, " Semester ", $sem, "<br>";\n  \n  // Using print\n  $status = print("Printing course status...<br>");\n  echo "Print returned: " . $status; // 1\n?>`,
    diagram: `PHP SERVER-SIDE EXECUTION ARCHITECTURE:
[ Web Browser ]
      |
      | 1. HTTP GET index.php
      v
[ Web Server (Apache / Nginx) ]
      |
      | 2. Passes to PHP-FPM / Zend Engine
      v
[ PHP Engine ] <====== 3. SQL Queries ======> [ MySQL Database ]
      |
      | 4. Returns Generated HTML String
      v
[ Web Server ]
      |
      | 5. HTTP Response (Plain HTML)
      v
[ Web Browser Renders Page ] (Zero PHP visible)`,
    followUpQuestions: [
      {
        question: 'Why are PHP opening tags required and what is the shorthand echo tag?',
        answer: '`<?php` tells the server to enter PHP mode. The shorthand echo tag `<?= $var ?>` is equivalent to `<?php echo $var; ?>`.'
      },
      {
        question: 'What happens if you omit the closing ?> tag at the end of a pure PHP file?',
        answer: 'It is recommended best practice by PSR standards! Omitting the closing `?>` prevents accidental whitespace or newlines from being transmitted, which causes "headers already sent" errors.'
      }
    ],
    quickRevision: 'PHP runs on the server and outputs plain HTML. `echo` returns void and takes multiple parameters; `print` returns 1 and takes one.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.1'
    },
    tags: ['php', 'echo', 'print', 'server-side', 'zend-engine']
  },
  {
    id: 'ce0522-u4-q2',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'basic',
    questionNumber: 'Q.2',
    question: 'Explain PHP Variables, Data Types, Constants, and Variable Scope (local, global, static).',
    shortAnswer: 'PHP variables begin with `$`, are case-sensitive, and dynamically typed. Constants are defined via `define()` or `const` and cannot be changed or redefined. Variable scope dictates accessibility: Local (inside functions), Global (accessible outside functions or via the `global` keyword / `$GLOBALS`), and Static (retains value between function invocations).',
    detailedAnswer: '1. PHP Data Types:\n   - Scalar types: `boolean`, `integer`, `float` (double), `string`.\n   - Compound types: `array`, `object`, `callable`, `iterable`.\n   - Special types: `NULL`, `resource` (e.g. database handle, file pointer).\n\n2. Defining Constants:\n   - `define("UNIVERSITY", "Indus University");` (runtime definition, can be conditional).\n   - `const PI = 3.14159;` (compile-time definition, valid in class bodies).\n   - Constants have no `$` prefix and are globally accessible.\n\n3. Variable Scope:\n   - Local Scope: Declared inside a function; accessible only inside that function.\n   - Global Scope: Declared outside functions. To access inside a function, must declare `global $var;` or use `$GLOBALS["var"]`.\n   - Static Scope: Declared with `static $count = 0;` inside a function. The variable is NOT destroyed when the function exits; it retains its value across repeated calls.',
    keyPoints: [
      'Variable names start with `$` (e.g., `$userName`).',
      'PHP is loosely typed; types are determined dynamically at runtime.',
      'Constants (`define()` / `const`) are immutable and have no `$` prefix.',
      '`static` variables preserve their value across successive function calls.',
      '`$GLOBALS` superglobal array provides global variables across any scope.'
    ],
    example: `<?php\nfunction trackVisits() {\n  static $visitCount = 0; // Preserved across calls\n  $visitCount++;\n  return $visitCount;\n}\n\necho trackVisits(); // 1\necho trackVisits(); // 2\necho trackVisits(); // 3\n?>`,
    diagram: `VARIABLE SCOPE IN PHP:
$globalVar = 100; // Global Scope
function test() {
  // $globalVar is INACCESSIBLE here unless 'global $globalVar;' is used!
  $localVar = 50;   // Local Scope (destroyed on return)
  static $stat = 0; // Static Scope (retained in memory)
}`,
    followUpQuestions: [
      {
        question: 'What is the difference between single quotes (\'\') and double quotes ("") in PHP strings?',
        answer: 'Double quotes parse and evaluate embedded variables (`"Hello $name"`) and escape sequences (`\\n`), whereas single quotes treat everything as literal text (`\'Hello $name\'` prints the literal string).'
      },
      {
        question: 'What is a PHP superglobal?',
        answer: 'Superglobals are built-in associative arrays always accessible in all scopes throughout the script without declaring `global` (e.g., `$_GET`, `$_POST`, `$_SESSION`, `$_COOKIE`, `$_SERVER`).'
      }
    ],
    quickRevision: 'Variables use `$`; constants (`define`) are immutable; `static` variables retain their values across repeated function calls.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.2'
    },
    tags: ['php-variables', 'constants', 'scope', 'static', 'data-types']
  },
  {
    id: 'ce0522-u4-q3',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'intermediate',
    questionNumber: 'Q.3',
    question: 'Explain PHP Arrays (Indexed, Associative, Multidimensional) and essential array functions.',
    shortAnswer: 'PHP arrays are versatile ordered maps associating values with keys. Indexed Arrays use numeric indices (0, 1, 2...). Associative Arrays use named string keys (`key => value`). Multidimensional Arrays contain one or more nested arrays. Essential functions include `count()`, `array_push()`, `in_array()`, `array_merge()`, `sort()`, and `ksort()`.',
    detailedAnswer: '1. Types of Arrays in PHP:\n   - Indexed Array: Auto-indexed from 0.\n     `$fruits = ["Apple", "Mango", "Banana"];`\n   - Associative Array: Named key-value pairs.\n     `$student = ["name" => "Manan", "sem" => 5, "cpi" => 9.1];`\n   - Multidimensional Array: Nested arrays modeling matrix/table structures.\n     `$catalog = [ ["CE0522", "WT"], ["CE0518", "CN"] ];`\n\n2. Iterating with `foreach`:\n   - Value only: `foreach ($fruits as $fruit) { ... }`\n   - Key and Value: `foreach ($student as $key => $val) { echo "$key: $val"; }`\n\n3. Key PHP Array Functions:\n   - `count($arr)`: Returns total elements.\n   - `array_push($arr, $item)`: Appends to end.\n   - `array_pop($arr)`: Removes last element.\n   - `in_array($val, $arr)`: Checks if value exists (boolean).\n   - `array_key_exists($key, $arr)`: Checks if key exists.\n   - `array_merge($arr1, $arr2)`: Combines arrays.\n   - Sorting: `sort()` (by value ascending), `rsort()` (value descending), `asort()` (associative by value), `ksort()` (associative by key).',
    keyPoints: [
      'Indexed arrays use numerical indices starting at 0.',
      'Associative arrays map descriptive string keys to values (`key => value`).',
      '`foreach ($arr as $key => $value)` is the primary loop for iterating arrays.',
      '`in_array()` tests value presence; `array_key_exists()` tests key presence.',
      '`ksort()` sorts associative arrays by key; `asort()` sorts by value.'
    ],
    example: `<?php\n$students = [\n  ["id" => 101, "name" => "Manan", "grade" => "AA"],\n  ["id" => 102, "name" => "Rohan", "grade" => "AB"]\n];\n\nforeach ($students as $s) {\n  echo "ID: " . $s["id"] . " | Name: " . $s["name"] . "<br>";\n}\n?>`,
    diagram: `PHP ARRAY TAXONOMY:
+-------------------------------------------------------------+
|                      PHP ARRAYS                             |
|                           |                                 |
|      +--------------------+--------------------+            |
|      v                    v                    v            |
| [ Indexed ]        [ Associative ]      [ Multidimensional ]|
|  $a[0] = 'Apple'    $s['name']='Manan'   $m[0]['title']     |
+-------------------------------------------------------------+`,
    followUpQuestions: [
      {
        question: 'What is the difference between sort() and asort() in PHP?',
        answer: '`sort()` sorts by value and re-indexes keys numerically (destroying original associative keys). `asort()` sorts by value while preserving original key-value associations.'
      },
      {
        question: 'How do you convert an array to JSON in PHP?',
        answer: 'Using `json_encode($array)`, and you convert JSON back to a PHP array using `json_decode($json, true)`.'
      }
    ],
    quickRevision: 'PHP arrays are ordered maps: Indexed (numeric keys), Associative (named keys), and Multidimensional. Iterated via `foreach`.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.3'
    },
    tags: ['php-arrays', 'associative-array', 'foreach', 'ksort', 'json_encode']
  },
  {
    id: 'ce0522-u4-q4',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'basic',
    questionNumber: 'Q.4',
    question: 'Compare HTTP GET vs POST methods in PHP with superglobals $_GET and $_POST.',
    shortAnswer: 'GET transmits data appended directly to the URL query string (`?key=val`), is visible in browser history, has a length limit (~2048 chars), and is cached; accessed in PHP via `$_GET`. POST transmits data securely inside the HTTP request body, is not displayed in the URL, supports unlimited payload sizes, can upload binary files, and cannot be bookmarked; accessed via `$_POST`.',
    detailedAnswer: '1. The GET Method:\n   - Parameters are visible in URL: `process.php?uname=Manan&sem=5`.\n   - Superglobal: `$_GET["uname"]`.\n   - Visibility & History: Bookmarked easily and preserved in browser history and server access logs.\n   - Size Limitation: Restricted by browser/server URL length limits (typically 2048 characters).\n   - Media: Plain text/ASCII only; CANNOT upload files.\n   - Security: Insecure for sensitive credentials (passwords visible in address bar).\n   - Idempotency: Safe and idempotent (used for searches and data retrieval).\n\n2. The POST Method:\n   - Parameters sent in HTTP request body (invisible in address bar).\n   - Superglobal: `$_POST["uname"]`.\n   - Visibility & History: Not saved in browser history or bookmarked.\n   - Size Limitation: No intrinsic protocol limit; controlled by server `post_max_size` and `upload_max_filesize` in `php.ini`.\n   - Media: Supports binary data, multimedia, and file uploads (`enctype="multipart/form-data"` via `$_FILES`).\n   - Security: Sensitive credentials are not leaked in URLs (must still use HTTPS for transport encryption).\n   - Idempotency: Non-idempotent (triggers database inserts/modifications).',
    keyPoints: [
      'GET appends parameters to URL (`$_GET`); POST encapsulates data in request body (`$_POST`).',
      'GET is limited to ~2048 characters; POST supports large payloads and file uploads.',
      'Never send passwords or sensitive credentials using GET.',
      'File uploads require `method="POST"` and `enctype="multipart/form-data"`.',
      '`$_REQUEST` combines `$_GET`, `$_POST`, and `$_COOKIE`.'
    ],
    example: `<!-- Form using POST for security -->\n<form action="login.php" method="POST">\n  <input type="text" name="user">\n  <input type="password" name="pass">\n  <button type="submit">Login</button>\n</form>\n\n<?php\n// In login.php:\nif ($_SERVER["REQUEST_METHOD"] === "POST") {\n  $user = htmlspecialchars($_POST["user"]);\n  $pass = $_POST["pass"];\n}\n?>`,
    diagram: `GET vs POST COMPARISON:
GET Request:
URL: http://site.com/search.php?query=web+tech&page=1
+------------------------------------+
| HTTP GET Headers                   | (Parameters exposed in URL)
+------------------------------------+
(Empty Body)

POST Request:
URL: http://site.com/login.php
+------------------------------------+
| HTTP POST Headers                  |
+------------------------------------+
| Body: user=manan&pass=secret123    | (Parameters hidden inside Body)
+------------------------------------+`,
    followUpQuestions: [
      {
        question: 'Why does refreshing a page after submitting a POST form prompt "Confirm Form Resubmission"?',
        answer: 'Because POST is non-idempotent. The browser warns the user that resending the request might repeat an action (e.g., charging a credit card twice). The Post/Redirect/Get (PRG) pattern solves this.'
      },
      {
        question: 'What superglobal contains data from GET, POST, and COOKIE combined?',
        answer: '`$_REQUEST`. However, using `$_GET` and `$_POST` explicitly is preferred for clarity and security.'
      }
    ],
    quickRevision: 'GET displays data in URL for retrieval (`$_GET`); POST sends data in request body for creation/updates (`$_POST`).',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.4'
    },
    tags: ['get', 'post', 'superglobals', 'forms', 'security']
  },
  {
    id: 'ce0522-u4-q5',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'intermediate',
    questionNumber: 'Q.5',
    question: 'Explain PHP Sessions and Cookies in detail. How do Sessions work under the hood?',
    shortAnswer: 'Cookies are stored client-side in the user\'s browser (set via `setcookie()`, max 4 KB). Sessions store data server-side in temp files or memory, identifying the client via a unique Session ID (`PHPSESSID`) transmitted in a cookie. Sessions are initiated with `session_start()`, manipulated via `$_SESSION`, and destroyed with `session_destroy()`. Sessions are much more secure than cookies for sensitive user state.',
    detailedAnswer: '1. How PHP Sessions Work Under the Hood:\n   - Step 1: `session_start()` is called at the very top of the PHP script before any HTML output.\n   - Step 2: The server checks if the incoming request has a cookie named `PHPSESSID`.\n   - Step 3: If no cookie exists, PHP generates a random 32-character hexadecimal Session ID, writes a session file on the server (e.g., `/tmp/sess_abc123...`), and sends a `Set-Cookie: PHPSESSID=abc123...` response header to the browser.\n   - Step 4: If the cookie exists, PHP reads the corresponding session file from disk and populates the `$_SESSION` superglobal array.\n   - Step 5: When the script ends, PHP serializes and writes `$_SESSION` data back to the server session file.\n\n2. Managing Sessions in PHP:\n   - Start: `session_start();`\n   - Store: `$_SESSION["userId"] = 42;`\n   - Read: `$uid = $_SESSION["userId"] ?? null;`\n   - Destroy / Logout:\n     ```php\n     session_start();\n     $_SESSION = array(); // Wipe session array\n     if (ini_get("session.use_cookies")) {\n       setcookie(session_name(), "", time() - 42000, "/");\n     }\n     session_destroy(); // Remove file from server\n     ```\n\n3. Setting Cookies in PHP:\n   `setcookie(name, value, expire, path, domain, secure, httponly);`\n   Example: `setcookie("user_pref", "dark", time() + (86400 * 30), "/", "", true, true);`',
    keyPoints: [
      '`session_start()` MUST be called before any HTML or whitespace output.',
      'Session data lives on the server; the browser only stores the session ID cookie (`PHPSESSID`).',
      'Cookies are stored on client browser (max 4 KB); Sessions are unlimited server storage.',
      '`session_regenerate_id(true)` prevents Session Fixation attacks upon login.',
      'Delete cookie by setting expiration timestamp in the past (`time() - 3600`).'
    ],
    example: `<?php\n// Protected Dashboard Page\nsession_start();\n\nif (!isset($_SESSION['authenticated'])) {\n  header("Location: login.php");\n  exit();\n}\n\necho "Welcome, " . htmlspecialchars($_SESSION['username']);\n?>`,
    diagram: `HOW PHP SESSIONS WORK UNDER THE HOOD:
Client (Browser)                           Web Server (PHP)
      |                                           |
      | ------ 1. First Visit (No Cookie) ------> | session_start()
      |                                           | Generates ID: "abc123"
      |                                           | Creates /tmp/sess_abc123
      | <----- 2. Set-Cookie: PHPSESSID=abc123 -- |
      |                                           |
      | (Stores PHPSESSID in cookie jar)          |
      |                                           |
      | ------ 3. Next Request (Cookie: abc123)-> | session_start()
      |                                           | Reads /tmp/sess_abc123
      |                                           | Populates $_SESSION!`,
    followUpQuestions: [
      {
        question: 'What error occurs if session_start() is called after an echo or HTML tag?',
        answer: '"Warning: session_start(): Cannot start session when headers already sent". Session start needs to send HTTP headers, which cannot happen after body output starts.'
      },
      {
        question: 'What is Session Hijacking and how can it be mitigated?',
        answer: 'Session Hijacking occurs when an attacker steals the `PHPSESSID` cookie. Mitigation: Use HTTPS (`Secure` flag), set `HttpOnly` on session cookies, regenerate session IDs upon login (`session_regenerate_id()`), and bind sessions to user IP and User-Agent.'
      }
    ],
    quickRevision: 'Cookies store 4KB on the client; Sessions store state on the server linked by a `PHPSESSID` cookie initiated by `session_start()`.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.5'
    },
    tags: ['sessions', 'cookies', 'session_start', 'session-hijacking', 'auth']
  },
  {
    id: 'ce0522-u4-q6',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'intermediate',
    questionNumber: 'Q.6',
    question: 'How do you prevent Cross-Site Scripting (XSS) and sanitize form inputs in PHP?',
    shortAnswer: 'Cross-Site Scripting (XSS) occurs when malicious JavaScript code is injected into web pages and executed in victim browsers. In PHP, XSS is prevented by sanitizing and escaping all user input before outputting to HTML using `htmlspecialchars($input, ENT_QUOTES, \'UTF-8\')`, `strip_tags()`, and validating data types with `filter_var()`.',
    detailedAnswer: '1. What is Cross-Site Scripting (XSS)?\n   If a user submits `<script>alert(document.cookie)</script>` in a comment box, and PHP outputs it unescaped as `echo $_POST[\'comment\'];`, the browser executes the script, allowing the attacker to hijack sessions.\n\n2. Key Sanitization and Escaping Functions:\n   - `htmlspecialchars($str, ENT_QUOTES, \'UTF-8\')`:\n     Converts special HTML characters into safe HTML entities:\n     * `&` -> `&amp;`\n     * `<` -> `&lt;`\n     * `>` -> `&gt;`\n     * `"` -> `&quot;`\n     * `\'` -> `&#039;`\n   - `trim($str)`: Strips unnecessary whitespace from beginning and end.\n   - `stripslashes($str)`: Removes unneeded backslashes.\n   - `filter_var($email, FILTER_VALIDATE_EMAIL)`: Validates email syntax strictly.\n   - `filter_var($int, FILTER_VALIDATE_INT)`: Validates integers.\n   - `strip_tags($str)`: Strips all HTML and PHP tags from a string.',
    keyPoints: [
      'XSS allows attackers to inject malicious client-side JavaScript.',
      'Never trust user input; sanitize on input, escape on output.',
      '`htmlspecialchars($val, ENT_QUOTES, "UTF-8")` is the primary XSS defense.',
      'Use `filter_var()` with `FILTER_VALIDATE_EMAIL` and `FILTER_VALIDATE_INT`.'
    ],
    example: `<?php\nfunction sanitizeInput($data) {\n  $data = trim($data);\n  $data = stripslashes($data);\n  $data = htmlspecialchars($data, ENT_QUOTES, 'UTF-8');\n  return $data;\n}\n\n$cleanName = sanitizeInput($_POST['student_name'] ?? '');\n$email = filter_var($_POST['email'] ?? '', FILTER_VALIDATE_EMAIL);\n\nif (!$email) {\n  die("Invalid email provided!");\n}\n?>`,
    diagram: `XSS ATTACK PREVENTION PIPELINE:
Attacker input: <script>stealCookies()</script>
                       |
                       v
         htmlspecialchars($input, ENT_QUOTES)
                       |
                       v
Safe HTML: &lt;script&gt;stealCookies()&lt;/script&gt;
                       |
                       v
Browser renders literal text without executing script!`,
    followUpQuestions: [
      {
        question: 'What is the difference between Stored XSS and Reflected XSS?',
        answer: 'Stored XSS permanently stores the malicious payload in the server database (e.g. forum comments), affecting every user who views the page. Reflected XSS reflects the payload immediately in the current HTTP response (e.g. search query parameter).'
      },
      {
        question: 'What HTTP response header adds modern browser-level XSS protection?',
        answer: 'Content Security Policy (CSP): `Content-Security-Policy: default-src \'self\';`'
      }
    ],
    quickRevision: 'Prevent XSS by transforming special characters into safe entities using `htmlspecialchars($data, ENT_QUOTES, "UTF-8")`.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.6'
    },
    tags: ['xss', 'security', 'htmlspecialchars', 'filter_var', 'sanitization']
  },
  {
    id: 'ce0522-u4-q7',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'advanced',
    questionNumber: 'Q.7',
    question: 'Explain Object-Oriented Programming (OOP) in PHP: Classes, Constructor, Destructor, Access Modifiers, and Inheritance.',
    shortAnswer: 'PHP supports full Object-Oriented Programming. Classes are declared with `class`, instantiated using `new`, and refer to instance members using `$this->property`. Constructors (`__construct()`) initialize objects; Destructors (`__destruct()`) clean up upon destruction. Access modifiers (`public`, `protected`, `private`) control visibility. Inheritance is implemented via the `extends` keyword.',
    detailedAnswer: '1. Core OOP Building Blocks in PHP:\n   - Class & Object: A blueprint and its in-memory instance.\n   - `$this`: Pseudo-variable referencing the current calling object instance.\n   - Constructor (`__construct()`): Magic method automatically called upon object instantiation via `new`.\n   - Destructor (`__destruct()`): Magic method invoked when object has no references or script terminates.\n\n2. Access Modifiers:\n   - `public`: Accessible from anywhere (inside class, subclasses, and external code).\n   - `protected`: Accessible only within the declaring class and its derived child subclasses.\n   - `private`: Accessible strictly within the declaring class only.\n\n3. Inheritance & Polymorphism:\n   - Child class inherits properties and methods using `class Child extends Parent`.\n   - Calling parent constructor: `parent::__construct(...)`.\n   - Method Overriding: Subclass replaces inherited implementation.\n   - `final` keyword: Prevents class inheritance or method overriding.\n   - Abstract Classes & Interfaces: Define method contracts that subclasses must implement.',
    keyPoints: [
      '`class` defines blueprints; instantiated with `new`.',
      '`__construct()` initializes instances; `__destruct()` runs on garbage collection.',
      'Access modifiers: `public` (anywhere), `protected` (class + children), `private` (class only).',
      'Use `$this->member` (arrow operator) to access properties and methods.',
      'Use `parent::method()` to invoke parent class methods.'
    ],
    example: `<?php\nclass Course {\n  protected $code;\n  protected $title;\n\n  public function __construct($code, $title) {\n    $this->code = $code;\n    $this->title = $title;\n  }\n\n  public function getDetails() {\n    return "{$this->code}: {$this->title}";\n  }\n}\n\nclass PracticalCourse extends Course {\n  private $labHours;\n\n  public function __construct($code, $title, $labHours) {\n    parent::__construct($code, $title);\n    $this->labHours = $labHours;\n  }\n\n  public function getDetails() {\n    return parent::getDetails() . " ({$this->labHours} Lab Hours)";\n  }\n}\n\n$wt = new PracticalCourse("CE0522", "Web Technology", 24);\necho $wt->getDetails();\n?>`,
    diagram: `PHP OOP INHERITANCE:
+------------------------------------+
|            Course                  |
| - $code, $title (protected)        |
| + __construct(), getDetails()      |
+-----------------+------------------+
                  ^
                  | extends
+-----------------+------------------+
|        PracticalCourse             |
| - $labHours (private)              |
| + getDetails() [Overridden]        |
+------------------------------------+`,
    followUpQuestions: [
      {
        question: 'What is the difference between an Interface and an Abstract Class in PHP?',
        answer: 'An abstract class can contain concrete implemented methods and properties as well as abstract methods. An interface can ONLY declare public method signatures and constants, with zero implementation bodies. A class can implement multiple interfaces but extend only one class.'
      },
      {
        question: 'What is the Scope Resolution Operator (::) in PHP?',
        answer: 'The Paamayim Nekudotayim (`::`) is used to access static methods, static properties, constants, and parent class methods (`parent::method()`).'
      }
    ],
    quickRevision: 'PHP OOP uses `class`, `new`, `__construct()`, access modifiers (`public`, `protected`, `private`), and inheritance with `extends`.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.7'
    },
    tags: ['oop', 'classes', 'inheritance', 'constructor', 'access-modifiers']
  },
  {
    id: 'ce0522-u4-q8',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'intermediate',
    questionNumber: 'Q.8',
    question: 'Explain Exception Handling in PHP using try, catch, finally, and custom Exceptions.',
    shortAnswer: 'PHP handles runtime exceptions using `try...catch...finally` blocks. Risky operations are enclosed in `try`. When an error occurs, an exception is raised with `throw new Exception("message", code)`. The `catch (Exception $e)` block intercepts it, providing `$e->getMessage()`, `$e->getCode()`, and `$e->getFile()`. The `finally` block always executes for resource cleanup.',
    detailedAnswer: '1. Exception Handling Architecture:\n   - `try`: Code block tested for exceptions.\n   - `catch (Throwable $e)`: Catches errors. In PHP 7+, `Throwable` is the base interface implemented by both `Exception` and `Error` (syntax/fatal errors).\n   - `finally`: Executes cleanup operations (e.g. closing database connections or open file handles).\n   - `throw`: Triggers an exception programmatically.\n\n2. Custom Exception Classes:\n   Developers can subclass PHP\'s base `Exception` to create domain-specific error types.',
    keyPoints: [
      '`try...catch...finally` prevents unhandled fatal crashes.',
      '`throw new Exception("msg")` raises exceptions.',
      'In PHP 7+, `Throwable` catches both `Exception` and fatal `Error` objects.',
      '`finally` block executes unconditionally.'
    ],
    example: `<?php\nclass DatabaseException extends Exception {}\n\nfunction connectDB($host) {\n  if ($host !== "localhost") {\n    throw new DatabaseException("Remote database connections disallowed!");\n  }\n  return "Connected successfully";\n}\n\ntry {\n  echo connectDB("remote.server.com");\n} catch (DatabaseException $e) {\n  echo "DB Error: " . $e->getMessage() . " on line " . $e->getLine();\n} finally {\n  echo "<br>Execution finished.";\n}\n?>`,
    diagram: `PHP EXCEPTION HANDLING FLOW:
   [ try Block ]
         |
    Exception thrown?
       /       \\
     YES        NO
      |          |
      v          |
[ catch(Exception $e) ] |
      |          |
      +-----+----+
            |
            v
     [ finally Block ] (Always executes)
            |
            v
   [ Continue Script ]`,
    followUpQuestions: [
      {
        question: 'What is the top-level interface for exceptions in PHP 7+?',
        answer: '`Throwable`. Catching `Throwable` catches both standard `Exception` instances and system `Error` exceptions (like `TypeError` and `ParseError`).'
      },
      {
        question: 'What happens if a thrown exception is not caught anywhere?',
        answer: 'PHP terminates script execution and outputs a "Fatal error: Uncaught Exception".'
      }
    ],
    quickRevision: 'Use `try...catch...finally` with `throw new Exception()` to intercept errors and prevent script termination.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.8'
    },
    tags: ['exceptions', 'try-catch', 'throwable', 'error-handling', 'php']
  },
  {
    id: 'ce0522-u4-q9',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'basic',
    questionNumber: 'Q.9',
    question: 'What is PHPMyAdmin? Explain its features and role in web database management.',
    shortAnswer: 'PHPMyAdmin is a free, open-source, web-based Graphical User Interface (GUI) written in PHP for managing MySQL and MariaDB databases over the web. It allows developers to create databases, design tables, run SQL queries, manage user permissions, and import/export `.sql` database dumps through an intuitive visual browser interface.',
    detailedAnswer: '1. Primary Features of PHPMyAdmin:\n   - Database & Table Management: Create, alter, drop, and optimize databases, tables, columns, indexes, and primary/foreign keys.\n   - Visual SQL Query Window: Write, execute, test, and bookmark SQL queries with syntax highlighting.\n   - Data Browsing & Editing: Insert, update, search, and delete individual table rows visually without writing SQL syntax.\n   - Import & Export: Export databases to `.sql`, CSV, XML, PDF, or JSON dumps, and import large database backups.\n   - User Privileges: Create MySQL user accounts, configure host permissions (`localhost` vs `%`), and assign granular permissions (`SELECT`, `INSERT`, `GRANT`).\n   - Visual Server Status: Displays server traffic, active connections, query statistics, and process monitor.\n\n2. Role in Web Development:\n   Comes pre-packaged with local LAMP, WAMP, XAMPP, and MAMP bundles, providing a seamless bridge between PHP scripts and MySQL storage.',
    keyPoints: [
      'Web-based GUI tool written in PHP for MySQL/MariaDB administration.',
      'Allows visual table creation, row editing, and running SQL queries.',
      'Supports exporting and importing `.sql` dump backups.',
      'Manages database users and access privileges.',
      'Bundled in local development stacks (XAMPP, WAMP, LAMP).'
    ],
    example: 'Accessing `http://localhost/phpmyadmin` in XAMPP allows creating a new database `student_portal` and running a script to create tables with auto-increment primary keys.',
    diagram: `PHPMYADMIN ARCHITECTURE:
[ Web Browser ]
      | (HTTP)
      v
[ Apache Web Server ] ===> [ phpMyAdmin PHP Scripts ] ===> [ MySQL Database Server ]`,
    followUpQuestions: [
      {
        question: 'Should phpMyAdmin be exposed with default "root" user with blank password on production servers?',
        answer: 'Never! That is a critical vulnerability. Production phpMyAdmin installations must be password-protected, placed behind VPNs or IP whitelists, or accessed via SSH tunnels.'
      },
      {
        question: 'What storage engines can you choose in phpMyAdmin when creating a table?',
        answer: 'InnoDB (default, supports ACID transactions and Foreign Keys) or MyISAM (older, fast full-text search, no foreign keys).'
      }
    ],
    quickRevision: 'PHPMyAdmin is a web-based GUI written in PHP for administering MySQL databases, running queries, and managing SQL dumps.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.9'
    },
    tags: ['phpmyadmin', 'mysql', 'gui', 'database-management', 'xampp']
  },
  {
    id: 'ce0522-u4-q10',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'advanced',
    questionNumber: 'Q.10',
    question: 'How do you connect PHP to MySQL? Compare MySQLi vs PDO and explain Prepared Statements for SQL Injection prevention.',
    shortAnswer: 'PHP connects to MySQL via MySQLi (MySQL Improved) or PDO (PHP Data Objects). Legacy `mysql_*` functions are obsolete. PDO is database-agnostic supporting 12+ database drivers; MySQLi is tailored specifically for MySQL. Prepared Statements prevent SQL Injection by separating SQL query code from user data parameters, ensuring user input is never executed as executable SQL commands.',
    detailedAnswer: '1. PHP MySQL Extension Comparison:\n   - Legacy `mysql_*`: Obsolete and completely removed in PHP 7.0.\n   - MySQLi (MySQL Improved):\n     * Supports only MySQL databases.\n     * Offers both Object-Oriented and Procedural APIs.\n     * Excellent performance and supports MySQL-specific features (e.g. multiple queries).\n   - PDO (PHP Data Objects):\n     * Database agnostic: Can switch between MySQL, PostgreSQL, SQLite, and Oracle by changing the DSN connection string.\n     * Purely Object-Oriented.\n     * Supports named parameters (`:username`).\n\n2. What is SQL Injection (SQLi)?\n   Occurs when user input is concatenated directly into SQL queries:\n   `$sql = "SELECT * FROM users WHERE user = \\"" . $_POST["user"] . "\\"";`\n   If attacker enters `\\\' OR \\\'1\\\'=\\\'1`, query evaluates to `WHERE user = \\\'\\\' OR \\\'1\\\'=\\\'1`, bypassing authentication!\n\n3. Prepared Statements Pipeline:\n   - Step 1: Prepare: SQL template sent to MySQL server with placeholders (`?` or `:name`). Server compiles the query structure.\n   - Step 2: Bind: User parameters are bound separately as raw data values.\n   - Step 3: Execute: Server executes the query with data values. Even if parameters contain quotes or SQL syntax, MySQL treats them strictly as plain literals, making SQL injection impossible.',
    keyPoints: [
      'Use MySQLi or PDO; legacy `mysql_*` is deprecated.',
      'PDO is database-agnostic; MySQLi is MySQL-specific.',
      'SQL injection happens when concatenating untrusted user input into queries.',
      'Prepared statements compile SQL structure first, then bind data parameters safely.',
      'Three-step pipeline: Prepare -> Bind Parameters -> Execute.'
    ],
    example: `<?php\n// Database Connection using PDO\n$dsn = "mysql:host=localhost;dbname=student_hub;charset=utf8mb4";\n$pdo = new PDO($dsn, "db_user", "db_pass", [\n  PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION,\n  PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC\n]);\n\n// Secure Prepared Statement (Prevents SQL Injection!)\n$stmt = $pdo->prepare("SELECT id, name, email FROM students WHERE semester = :sem");\n$stmt->execute([':sem' => 5]);\n$students = $stmt->fetchAll();\n\nforeach ($students as $row) {\n  echo $row['name'] . "<br>";\n}\n?>`,
    diagram: `PREPARED STATEMENT PIPELINE (SQLi IMMUNITY):
PHP Script                              MySQL Database Engine
    |                                              |
    | 1. prepare("SELECT * FROM u WHERE id = ?") ->| Compiles query plan & syntax tree
    |                                              | (Query structure is FIXED)
    |                                              |
    | 2. execute([ "' OR '1'='1" ]) -------------> | Injects value strictly as data literal
    |                                              | (Cannot alter compiled syntax tree!)
    |                                              |
    | <------- 3. Returns Safe Query Results ------|`,
    followUpQuestions: [
      {
        question: 'What do the parameter types "ssi" represent in mysqli_stmt_bind_param()?',
        answer: 'They specify the data types of the bound parameters: "s" for string, "i" for integer, "d" for double/float, and "b" for blob.'
      },
      {
        question: 'Why should you set PDO::ATTR_EMULATE_PREPARES to false in PDO?',
        answer: 'To enforce true native prepared statements on the MySQL server level rather than client-side string emulation.'
      }
    ],
    quickRevision: 'Connect using PDO or MySQLi. Prepared statements separate query code from data, completely neutralizing SQL Injection attacks.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.10'
    },
    tags: ['pdo', 'mysqli', 'sql-injection', 'prepared-statements', 'database']
  },
  {
    id: 'ce0522-u4-q11',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'intermediate',
    questionNumber: 'Q.11',
    question: 'How do you fetch and process MySQL database records in PHP to render dynamic HTML tables?',
    shortAnswer: 'Records are fetched from a query result using fetch methods (`mysqli_fetch_assoc()`, `mysqli_fetch_array()`, or PDO `fetch(PDO::FETCH_ASSOC)`). In PHP, we loop through the result set row-by-row using a `while` loop or `foreach`, sanitizing values with `htmlspecialchars()`, and outputting corresponding `<tr>` and `<td>` HTML elements dynamically.',
    detailedAnswer: '1. Result Fetching Functions in MySQLi:\n   - `mysqli_fetch_assoc($result)`: Returns next row as an associative array with column names as keys.\n   - `mysqli_fetch_row($result)`: Returns next row as a zero-indexed array.\n   - `mysqli_fetch_array($result, MYSQLI_BOTH)`: Returns both associative and numeric keys.\n   - `mysqli_num_rows($result)`: Returns the total count of rows matched.\n\n2. Looping Pattern:\n   ```php\n   $result = mysqli_query($conn, "SELECT id, name, branch FROM students");\n   if (mysqli_num_rows($result) > 0) {\n     while ($row = mysqli_fetch_assoc($result)) {\n       // Render row\n     }\n   }\n   ```\n\n3. Memory Management:\n   Always free result memory using `mysqli_free_result($result)` and close database handle via `mysqli_close($conn)` or let PDO close when the handle goes out of scope.',
    keyPoints: [
      '`mysqli_fetch_assoc()` returns associative array of column names and values.',
      'Loop over rows using `while ($row = mysqli_fetch_assoc($result))`.',
      'Always escape output with `htmlspecialchars()` to prevent stored XSS.',
      'Check `mysqli_num_rows($res) > 0` before rendering tables.'
    ],
    example: `<?php\n$conn = mysqli_connect("localhost", "root", "", "college_db");\n$res = mysqli_query($conn, "SELECT roll_no, name, gpa FROM students ORDER BY gpa DESC");\n?>\n<table border="1">\n  <tr><th>Roll No</th><th>Name</th><th>GPA</th></tr>\n  <?php while ($row = mysqli_fetch_assoc($res)): ?>\n    <tr>\n      <td><?= htmlspecialchars($row['roll_no']) ?></td>\n      <td><?= htmlspecialchars($row['name']) ?></td>\n      <td><?= htmlspecialchars($row['gpa']) ?></td>\n    </tr>\n  <?php endwhile; ?>\n</table>\n<?php mysqli_close($conn); ?>`,
    diagram: `DATA PROCESSING PIPELINE:
MySQL Server ---> Buffer Result Set ---> mysqli_fetch_assoc() ---> Row Associative Array ---> HTML <tr><td>`,
    followUpQuestions: [
      {
        question: 'What does mysqli_fetch_assoc() return when there are no more rows left in the result set?',
        answer: 'It returns boolean `null` (or `false`), which naturally terminates the `while` loop condition.'
      },
      {
        question: 'Why should you avoid SELECT * in production queries?',
        answer: '`SELECT *` retrieves all columns, wasting network bandwidth, database memory, and increasing query execution time. Always explicitly list required columns (e.g. `SELECT id, name`).'
      }
    ],
    quickRevision: 'Fetch database records with `mysqli_fetch_assoc()` inside a `while` loop and render dynamic HTML table rows with `htmlspecialchars()`.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.11'
    },
    tags: ['mysqli_fetch_assoc', 'html-tables', 'mysql-query', 'database-loop', 'php']
  },
  {
    id: 'ce0522-u4-q12',
    subjectCode: 'CE0522',
    subjectName: 'Web Technology',
    department: 'CE/IT/CSE',
    semester: 5,
    section: 'Unit 4: Server-Side PHP, Session Handling, MySQL Database & Web Hosting',
    category: 'theory',
    difficulty: 'intermediate',
    questionNumber: 'Q.12',
    question: 'Explain Web Hosting concepts, Domain Name System (DNS), Web Servers (Apache vs Nginx), and deployment.',
    shortAnswer: 'Web Hosting is the service of allocating space on a physical server connected to the internet to store website files and databases. A Domain Name (e.g., `campus.edu`) is translated into a server IP address by DNS (Domain Name System) using records (A, CNAME, MX). Web servers like Apache and Nginx process incoming HTTP requests and serve files. Deployment involves publishing code via FTP/SFTP, Git, or CI/CD pipelines and configuring SSL/TLS certificates.',
    detailedAnswer: '1. Types of Web Hosting:\n   - Shared Hosting: Hundreds of sites share one physical server and OS (low cost, limited resources/cPanel).\n   - VPS (Virtual Private Server): Physical server is partitioned via virtualization into dedicated virtual machines with root access.\n   - Dedicated Server: An entire physical server dedicated to a single customer.\n   - Cloud Hosting (AWS, GCP, DigitalOcean): Highly available cluster of virtual servers with auto-scaling.\n\n2. DNS (Domain Name System) & Record Types:\n   - \'A\' Record: Maps domain name directly to an IPv4 address (e.g., `campus.edu -> 192.0.2.1`).\n   - \'AAAA\' Record: Maps domain to an IPv6 address.\n   - \'CNAME\' (Canonical Name): Alias pointing one domain to another domain (e.g., `www.campus.edu -> campus.edu`).\n   - \'MX\' Record: Directs email traffic to mail servers.\n   - \'TXT\' Record: Used for domain verification, SPF, and DKIM email security.\n\n3. Web Servers: Apache vs Nginx:\n   - Apache: Process/thread-based architecture (`mpm_prefork`, `mpm_worker`). Highly configurable per directory using `.htaccess` files.\n   - Nginx: Asynchronous, event-driven, non-blocking architecture. Capable of handling 10,000+ concurrent connections with low memory usage; widely used as a Reverse Proxy and load balancer.\n\n4. Deployment & SSL/TLS:\n   - Code upload: Via SFTP or automated Git webhooks.\n   - HTTPS: Enabled using SSL/TLS certificates (e.g., free via Let\'s Encrypt) to encrypt HTTP traffic on port 443.',
    keyPoints: [
      'Hosting stores website files on servers connected to high-speed internet.',
      'DNS translates human-readable domains into machine IP addresses.',
      'A Record = Domain to IP; CNAME = Domain alias; MX = Mail server.',
      'Apache uses process threads and `.htaccess`; Nginx uses event-driven architecture.',
      'SSL/TLS encrypts traffic over port 443 for HTTPS security.'
    ],
    example: 'Purchasing a domain `studenthub.in` on GoDaddy, setting its DNS A Record to points to a DigitalOcean server IP `165.22.45.10`, configuring Nginx as a reverse proxy, and deploying via Git.',
    diagram: `DNS RESOLUTION & WEB HOSTING FLOW:
User enters "example.com"
         |
         v
DNS Server resolves "example.com" ===> IP Address: 93.184.216.34
         |
         v
Browser connects to Server IP on Port 443 (HTTPS)
         |
         v
Web Server (Nginx / Apache) processes request
         |
         +---> Serves Static Files (HTML/CSS/JS)
         +---> Passes to PHP / MySQL Database Engine`,
    followUpQuestions: [
      {
        question: 'What is the role of an .htaccess file in Apache?',
        answer: '`.htaccess` is a directory-level configuration file in Apache used for URL rewriting (mod_rewrite), custom 404 error pages, password protection, and security rules without modifying global server configs.'
      },
      {
        question: 'What is a Reverse Proxy?',
        answer: 'A reverse proxy (like Nginx) sits in front of internal web/application servers, intercepting client requests to provide load balancing, SSL termination, caching, and security shielding.'
      }
    ],
    quickRevision: 'Web hosting allocates server resources. DNS maps domain names to IPs (A, CNAME records). Apache and Nginx serve HTTP/HTTPS requests.',
    source: {
      type: 'syllabus',
      name: 'CE0522 Web Technology Unit 4 Syllabus',
      questionNumber: 'Q.12'
    },
    tags: ['web-hosting', 'dns', 'apache', 'nginx', 'ssl', 'deployment']
  }
];
